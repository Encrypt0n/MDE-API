name: .NET with SonarCloud

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Test with coverage
      run: |
        dotnet tool install --global coverlet.console
        export PATH="$PATH:$HOME/.dotnet/tools"
        dotnet build --no-incremental
        coverlet ./MDE_API.Tests/bin/Debug/net8.0/MDE_API.Tests.dll \
        --target "dotnet" \
        --targetargs "test --no-build --no-restore" \
        --format opencover \
        --output ./TestResults/coverage.opencover.xml

    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@v5.0.0
      with:
        args: >
          -Dsonar.organization=encrypt0n
          -Dsonar.projectKey=Encrypt0n_MDE-API
          -Dsonar.cs.opencover.reportsPaths=TestResults/coverage.opencover.xml
      env:
        SONAR_TOKEN: 43481cfa073dc3c47dfbba6d8cb7de73495730ad
